[tool:pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts =
    --verbose
    --tb=short
    --strict-markers
markers =
    api: Tests de API (endpoints HTTP)
    service: Tests de servicios (logica del proyecto)
    repository: Tests de repositorios (pruebas de repositorios)