version: '3.8'

services:

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 5467
      POSTGRES_DB: proyecto-pruebas
    ports:
      - "5433:5432"

  api:
    build: .
    depends_on:
      - db
    ports:
      - "8000:8000"